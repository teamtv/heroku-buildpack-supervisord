#!/usr/bin/env bash
# Require supervisord.conf to be present in the app directory.
# CNB calls this script with cwd = app source root.
if [[ -f supervisord.conf ]]; then
    exit 0
fi

echo "ERROR: supervisord.conf not found in app root" >&2
exit 1
